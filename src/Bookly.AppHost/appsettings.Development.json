{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "DevTunnel": {
    // Set to true to expose bookly-ui externally via Microsoft Dev Tunnels.
    // Requires: devtunnel CLI installed + `devtunnel login` executed once.
    // Alternatively, activate via user secrets: dotnet user-secrets set "DevTunnel:Enabled" "true"
    "Enabled": false
  }
}
